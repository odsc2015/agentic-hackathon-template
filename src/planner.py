def plan_tasks(user_input):
    return [
        "search trusted sources for claim verification",
        "summarize evidence from sources",
        "generate verdict with confidence level (True/False/Unverified)"
    ]